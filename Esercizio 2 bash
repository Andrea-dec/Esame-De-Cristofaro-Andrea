function file_count { 
 dir="$1"
 if [[ -d "$dir" ]]; then
   echo "Directory exists."
   cd $dir 
   
   else
  echo "Directory doesn't exists."
  echo 'exit status:' $(($?+1))
fi
}

file_count '/filepath'
